/**
 * Props interface for the ProjectsToggleButton component
 */
export interface ProjectsToggleButtonProps {
  /**
   * Boolean indicating whether the associated projects section is expanded
   * Controls which icon and text to display (plus or close)
   */
  isExpanded: boolean;
}
